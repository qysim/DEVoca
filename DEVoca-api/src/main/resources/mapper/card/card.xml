<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.devoca.card.model.mapper.CardMapper">

    <!--  카드 등록  -->
    <select id="registerCard" parameterType="CardDTO">
        insert into cards (fk_card_user_idx, card_word_id, card_content, card_link, card_access)
        values((select user_idx from users where user_id = #{userId}), #{wordId}, #{cardContent}, #{cardLink}, #{cardAccess});
    </select>

</mapper>