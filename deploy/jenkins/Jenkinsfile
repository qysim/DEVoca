pipeline {
    agent any

    stages {
        stage('Build API server') {
            steps {
                dir('deploy') {
                    sh 'echo "Build API server"'
                    sh 'docker-compose build api'
                }
            }
        }
    }
}
